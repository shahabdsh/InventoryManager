import { FormControlInvalidCheckerDirective } from './form-control-invalid-checker.directive';

describe('FormControlInvalidCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlInvalidCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
